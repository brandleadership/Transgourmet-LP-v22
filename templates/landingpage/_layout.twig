<!DOCTYPE html>
<html lang="en">
<head>
  <title>{{ properties.title }}</title>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link rel="stylesheet" href="{{ bsi_cx_css_href() }}"/>
   <link rel="icon" href="/favicon.ico" type="image/x-icon"
            href="{{ bsi_cx_asset(
                '../resources/img/favicon-TG.ico'
            )
            }}" />
          <link rel="apple-touch-icon"
            sizes="57x57"
              type="image/png"
            href="{{
            bsi_cx_asset(
                '../resources/img/favicon-TG.png'
            )
            }}" />
        <link rel="icon"
            type="image/png"
            sizes="192x192"
            href="{{
            bsi_cx_asset(
                '../resources/img/favicon-TG.png'
            )
            }}" />
         <link rel="icon"
            type="image/png"
            sizes="16x16"
            href="{{
            bsi_cx_asset(
               '../resources/img/favicon-TG.png'
            )
            }}" />
             <link rel="icon"
            type="image/png"
            sizes="16x16"
            href="{{
            bsi_cx_asset(
               '../resources/img/favicon-TG.png'
            )
            }}" />
               <link rel="icon"
            type="image/png"
            sizes="512x512"
            href="{{
            bsi_cx_asset(
               '../resources/img/favicon-TG.png'
            )
            }}" /> 
</head>
<body>

<main data-bsi-dropzone="a1683342-d4a7-4c26-924e-bce162c4399f">
  {% block main %}{% endblock %}
</main>


<footer>
  Built for BSI CX {{ cx.version.TARGET.toString() }}
</footer>

<script src="{{ bsi_cx_js_module_runtime_href() }}" defer="defer"></script>
<script src="{{ bsi_cx_js_module_href('main') }}" defer="defer"></script>
{{ bsi_cx_js_module_missing_chunks_import() }}

</body>
</html>
